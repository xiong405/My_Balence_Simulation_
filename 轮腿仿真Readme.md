# Webots 轮腿机器人仿真简要说明

## 一、项目目标

本项目使用 SolidWorks 导出的五连杆轮腿机器人模型，在 Webots 中完成腿长控制、直立平衡、移动和原地转向。当前只使用自己的 `TestModelAssembly` 模型，不再依赖开源项目原有的机器人模型。

## 二、模型与文件

当前 `.wbt` 模型使用 Webots R2025a，重力为 `9.8 m/s²`，基本仿真步长为 `2 ms`。机器人节点已经设置：

```text
name       "TestModelAssembly"
controller "my_controller"
supervisor TRUE
```

主要控制文件如下：

| 文件 | 作用 |
| --- | --- |
| `my_controller.py` | 读取传感器、构造状态量并输出电机力矩 |
| `leg.py` | 五连杆正运动学、速度解算和 VMC 映射 |
| `mymath.py` | 编码器差分等数学计算 |
| `lagrange.py` | 建模和离线计算 LQR 增益，当前控制循环不直接调用 |

## 三、设备名称

模型中共有 4 个髋关节电机和 2 个轮毂电机：

| 左侧 | 右侧 |
| --- | --- |
| `Left_Front_Motor` | `Right_Front_Motor` |
| `Left_Back_Motor` | `Right_Back_Motor` |
| `Left_Wheel` | `Right_Wheel` |

每个电机都有同名的 `_Sensor` 位置传感器。模型还包含 `imu`、`gyro` 和加速度计；当前代码实际使用 `imu`、`gyro` 和 6 个位置传感器。

## 四、启动方法

1. 将控制器文件放入：

   ```text
   项目目录/controllers/my_controller/
   ```

2. 确保 `my_controller.py`、`leg.py` 和 `mymath.py` 位于控制器目录中。
3. 用 Webots 打开 `.wbt` 世界文件。
4. 确认机器人控制器为 `my_controller`，然后点击运行。

代码使用 `Supervisor()`，所以模型中的 `supervisor TRUE` 不能删除。启动时机器人会先锁定并采集一拍传感器初值，然后解锁进入闭环控制。

## 五、当前控制流程

```text
IMU、陀螺仪和编码器
        ↓
五连杆运动学与速度解算
        ↓
构造 10 维状态量
        ↓
根据左右腿长重建 4×10 LQR 增益 K
        ↓
计算左右轮力矩和左右虚拟腿摆动力矩
        ↓
腿长 PD + VMC 映射
        ↓
6 个电机力矩输出
```

10 个状态的顺序为：

```text
s、速度、yaw、yaw 角速度、左腿角度、左腿角速度、
右腿角度、右腿角速度、机体 pitch、pitch 角速度
```

当前有效代码使用离线拟合的 `K_out` 系数，根据左右腿长实时重建 LQR 增益，更接近以后在单片机上的实现方式。

## 六、主要参数

| 参数 | 当前值 |
| --- | --- |
| 五连杆长度 | `0.21、0.25、0.25、0.21 m` |
| 轮子半径 | `0.05995 m` |
| 腿长目标 | `0.160 m → 0.250 m` |
| 腿长 PD | `Kp=300，Kd=40` |
| 单腿重力前馈 | `22 N` |
| 腿部力限幅 | `±40 N` |
| 髋关节力矩限幅 | `±10 N·m` |
| 轮子力矩限幅 | `±0.50 N·m` |

## 七、方向与极性

- 机器人前进方向为 Webots 世界坐标的 `+X` 轴。
- 左轮编码器直接读取，右轮编码器在代码中取负。
- 左轮力矩直接输出，右轮力矩输出时取负。
- 右侧两个髋关节的最终力矩也取负，以适配左右镜像安装。
- 所有方向修正应集中在传感器和电机映射处，避免重复取反。

## 八、当前测试时序

- `0～2 s`：目标位移为 `0.1 m`。
- `2～2.5 s`：腿长平滑升到 `0.25 m`，其余目标为零。
- `2.5 s` 后：目标 `yaw` 角速度为 `5 rad/s`，用于测试原地连续旋转；左右腿角目标均为 `0.1 rad`。

## 九、调试顺序

1. 模型能否正常落地，关节和轮子是否可转动。
2. IMU、陀螺仪和编码器数据是否正常。
3. 给单个电机小力矩，确认正负方向。
4. 先测试腿长 PID，再测试基础平衡 LQR。
5. 最后测试移动和原地转向。
